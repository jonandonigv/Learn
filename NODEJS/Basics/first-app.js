// Imports fs
const fs = require('fs');
// creates a file calling the method from fs 
fs.writeFileSync('hello.txt', 'Hello from nodejs');
// Outputs the msg to the terminal  
console.log("Hello world");
